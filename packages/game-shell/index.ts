// Reusable Game Shell — leaderboards, shareable score links, Discord auth,
// analytics. Implemented incrementally as games need them; empty on purpose
// until the first real game pulls in its first shell feature.
export {};
